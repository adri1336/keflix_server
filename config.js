const
    secretKey = "h<w}R2ZWan^~2y+y",
    authHeader = "access-token",
    saltRounds = 10,
    tokenExpiresIn = "1d"; //1 day

module.exports = {
    secretKey,
    authHeader,
    saltRounds,
    tokenExpiresIn
};